console.log("StartUp initiated");

// Called once when the page loads
window.resizeCanvas();
// start at 0° (pointing to the right)
window.setAngle0FromDeg(13);   // J0 
window.setAngle1FromDeg(160);   // J1 (relative) 
window.setAngle2FromDeg(40);   // J2 (relative)
